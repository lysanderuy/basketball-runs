# Supabase — browser client + auth (public, safe to expose)
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_<key>

# Drizzle ORM — PostgreSQL connection via Supabase
# Transaction pooler (port 6543) — used by Next.js serverless functions
DATABASE_URL="postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true"
# Direct connection (port 5432) — used by drizzle-kit generate/migrate
DIRECT_URL="postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres"

# Resend — transactional email (welcome) via the SDK
RESEND_API_KEY=re_<key>
# Sender on a Resend-verified domain (SPF + DKIM)
RESEND_FROM_EMAIL=BallRuns <hello@mail.yourdomain.com>
