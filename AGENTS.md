# BallRuns — Agent Instructions

The canonical instructions for all AI agents in this repo live in
[CLAUDE.md](./CLAUDE.md). Read that file — it is the single source of truth for
stack, project structure, data-flow rules, schema rules, and migrations.

This file exists only so tools that look for `AGENTS.md` are routed there. Do
not duplicate rules here — update CLAUDE.md instead, so the two can never drift
apart.
